kind: Service
apiVersion: v1
metadata:
  name: apache-service
  namespace: appache
spec: 
  selector:
    app: apache
  ports: 
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
